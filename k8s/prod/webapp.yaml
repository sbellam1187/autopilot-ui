apiVersion: runway.aa.com/v1alpha1
kind: WebApp
metadata:
  name: autopilot
  annotations:
    enableCloudIngress: "true"
    backstageName: autopilot-ui
  labels:
    app-id: autopilot-ui
    aa-app-shortname: APIMgmtGQL
    data-classification: nonpci
    environment: prod
spec:
  containerName: docker.aa.com/prod/autopilot-ui
  containerTag: 'v0.1.0'
  port: 3000
  healthCheck:
    endpoint: /
    enabled: False
    port: 3000
  secretProviders:
    - vault:
        vaultNamespace: automation/apimgmtgql-autopilot-p
        secretEngineMount: secrets
        secretPath: prod
  autoscale:
    minimum: 1
    maximum: 2
  reachableServices:
    - autopilot-agents.autopilot-prod.svc.8000.mesh